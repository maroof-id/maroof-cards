# 🚀 دليل الاستخدام بعد التحسينات

## تشغيل المشروع

### 1. تشغيل الخادم
```bash
python3 tools/web_app.py
```

**النتيجة المتوقعة:**
```
Initializing NFC reader...
✅ NFC reader ready!
 * Running on http://0.0.0.0:5001
```

أو:
```
Initializing NFC reader...
⚠️ NFC reader not connected - some features may not work
 * Running on http://0.0.0.0:5001
```

### 2. الوصول للموقع
- محلي: `http://localhost:5001`
- شبكة: `http://raspberrypi.local:5001`
- أي IP: `http://<IP>:5001`

---

## الاستخدام على الجوال 📱

### الموقع يشتغل حسن على:
- ✅ iPhone (Safari)
- ✅ Android (Chrome)
- ✅ أي جوال حديث

### الخطوات:
1. **الرئيسية** - إنشاء بطاقة جديدة
2. **الإعدادات** - قراءة بطاقات موجودة

---

## إنشاء بطاقة جديدة

### الخطوات:
1. اضغط على **الرئيسية**
2. ملأ البيانات (الاسم مطلوب):
   - الاسم الكامل *
   - رقم الجوال
   - البريد الإلكتروني
   - Instagram
   - LinkedIn
   - Twitter/X
   - نبذة تعريفية
   - اختر القالب

3. اضغط **إنشاء البطاقة**
4. انتظر الـ loading spinner (عادة 2-3 ثواني)

### النتائج الممكنة:

#### ✅ نجاح:
```
✅ تمت الإنشاء بنجاح!
الرابط: https://maroof-id.github.io/maroof-cards/mohammed
[كتابة على NFC]
```

#### ❌ خطأ:
```
❌ حدث خطأ
الاسم مطلوب / Name is required
```

---

## كتابة على NFC

### الخطوات:
1. أنشئ بطاقة بنجاح
2. اضغط **كتابة على NFC**
3. ضع البطاقة على القارئ
4. انتظر الرسالة

### النتائج الممكنة:

#### ✅ نجاح (بعد 2-5 ثواني):
```
✅ تمت الكتابة على البطاقة بنجاح!
```

#### ⏰ انتظار (يبحث عن البطاقة):
```
جاري الكتابة...
[loading spinner]
```

#### ❌ خطأ (بعد 15 ثانية):
```
❌ انتهت مهلة انتظار قارئ NFC
```

#### ❌ خطأ (القارئ غير متصل):
```
❌ قارئ NFC غير متصل
```

---

## قراءة بطاقة NFC

### الخطوات:
1. اضغط على **الإعدادات**
2. اضغط **قراءة بطاقة NFC**
3. ضع البطاقة على القارئ
4. انتظر النتيجة

### النتائج الممكنة:

#### ✅ نجاح:
```
✅ تمت القراءة بنجاح
{
  "uid": "04a1b2c3d4e5f6g7",
  "url": "https://maroof-id.github.io/maroof-cards/mohammed"
}
```

#### ❌ خطأ:
```
❌ خطأ في القراءة
انتهت مهلة انتظار قارئ NFC
```

---

## الرسائل المتوقعة

### نجاح ✅
- `✅ تمت الإنشاء بنجاح!`
- `✅ تمت الكتابة على البطاقة بنجاح!`
- `✅ تمت القراءة بنجاح`

### خطأ ❌
- `❌ قارئ NFC غير متصل`
- `❌ انتهت مهلة انتظار قارئ NFC`
- `❌ الاسم مطلوب`
- `❌ خطأ في الاتصال`

### تحذير ⚠️
- `⚠️ البطاقة لا تحتوي بيانات NDEF`
- `⚠️ لا توجد تغييرات`

### معلومات ℹ️
- `ℹ️ جاري إنشاء البطاقة...`
- `ℹ️ جاري الكتابة...`
- `ℹ️ جاري قراءة البطاقة...`

---

## الأوقات المتوقعة (Timeouts)

### إنشاء بطاقة:
- النموذج: فوري
- الحفظ: 1-2 ثانية
- Git push: 5-10 ثواني (في الخلفية)
- **المجموع**: 2-3 ثواني (feedback للمستخدم)

### كتابة على NFC:
- البحث عن البطاقة: حتى 15 ثانية
- الكتابة: 1-2 ثانية
- Git push: 5-10 ثواني (في الخلفية)
- **المجموع**: 2-5 ثواني (feedback فوري)

### قراءة بطاقة NFC:
- البحث عن البطاقة: حتى 15 ثانية
- القراءة: 1-2 ثانية
- **المجموع**: 2-5 ثواني (feedback فوري)

---

## استكشاف الأخطاء 🔧

### المشكلة: NFC reader بطيء أو معطل
**الرسالة:**
```
❌ انتهت مهلة انتظار قارئ NFC
```

**الحل:**
1. تحقق من الـ USB cable
2. جرب إعادة تشغيل القارئ
3. جرب port مختلف
4. تأكد من permissions: `sudo`

### المشكلة: البطاقة لا تُكتب
**الرسالة:**
```
❌ خطأ في الكتابة
```

**الحل:**
1. تأكد البطاقة NTAG215
2. جرب بطاقة مختلفة
3. تأكد من اتصال القارئ

### المشكلة: Offline - GitHub غير متصل
**الرسالة:**
```
✅ تمت الإنشاء بنجاح! (لكن تحذير في الخلفية)
```

**الحل:**
1. البطاقة تُحفظ محلياً بنجاح ✅
2. ستُرفع لـ GitHub عند الاتصال
3. تابع العمل بدون قلق

### المشكلة: الموقع ما يظهر على الجوال
**الحل:**
1. تأكد من الـ IP الصحيح
2. تأكد من الـ WiFi نفس الـ network
3. جرب مع `localhost:5001` لو في نفس الجهاز

---

## الإعدادات المتقدمة

### تغيير الـ Port:
**الملف:** `tools/web_app.py`

ابحث عن:
```python
app.run(host='0.0.0.0', port=5001, debug=False)
```

غيّر `5001` إلى port مختلف:
```python
app.run(host='0.0.0.0', port=5002, debug=False)
```

### تغيير الـ Timeout:
**الملف:** `tools/web_app.py` و `tools/nfc_writer.py`

للـ NFC:
```python
writer.write_url(url, timeout=20)  # من 15 إلى 20 ثانية
writer.read_card(timeout=20)       # من 15 إلى 20 ثانية
```

للـ Git:
```python
self.git_push(message, timeout=60)  # من 30 إلى 60 ثانية
```

---

## Best Practices 💡

### عند الاستخدام:
1. ✅ استخدم أسماء عربية أو إنجليزية
2. ✅ رقم الجوال بصيغة 05xxxxxxxx
3. ✅ انتظر الرسالة قبل تكرار الضغط
4. ✅ ضع البطاقة بشكل صحيح على القارئ

### عند حدوث مشكلة:
1. ✅ اقرأ الرسالة بعناية
2. ✅ انتظر الـ timeout الكامل
3. ✅ جرب مرة أخرى
4. ✅ تحقق من الـ Hardware

### البيانات المحفوظة:
```
clients/USERNAME/
├── index.html      ← صفحة البطاقة
├── data.json       ← البيانات الخام
└── contact.vcf     ← vCard للجوال
```

---

## أسئلة شائعة ❓

### س: ليش الموقع يأخذ وقت أول مرة؟
**ج:** لأنه بينشئ ملفات وبيدفعها لـ GitHub. العمليات التالية أسرع.

### س: إذا انقطع الإنترنت أثناء الكتابة؟
**ج:** البطاقة تُكتب بنجاح لكن البيانات ما تطلع لـ GitHub. ستطلع لما تتصل.

### س: إذا حصل crash أثناء الكتابة؟
**ج:** البطاقة قد تكون نصفها مكتوب. جرب كتابة جديدة.

### س: هل بيانات البطاقة تُحفظ بأمان؟
**ج:** الملفات بتُحفظ على الـ Raspberry Pi محلياً. حافظ على الجهاز آمن.

### س: شنو أقصى حد للأسماء والبيانات؟
**ج:** لا حد أقصى محدد، لكن أفضل استخدم أسماء وبيانات معقولة.

---

## نصائح الأداء ⚡

### لتسريع الموقع:
1. ✅ استخدم شبكة WiFi جيدة
2. ✅ قرب الجوال من الـ Raspberry Pi
3. ✅ تأكد من اتصال Git جيد

### لتجنب الأخطاء:
1. ✅ استخدم NFC cards عالية الجودة (NTAG215+)
2. ✅ نظّف رأس القارئ بشكل منتظم
3. ✅ تأكد من تحديثات البرامج

---

**الآن المشروع جاهز للاستخدام! استمتع 🎉**
